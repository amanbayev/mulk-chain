{
  "name": "@mulk-chain/core-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Identity hub, API gateway, batch auction, DvP T+0 and rental yield",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "vitest run test/e2e-full-loop.spec.ts"
  },
  "dependencies": {
    "ethers": "^6.13.4",
    "hono": "^4.7.4",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^3.0.5"
  }
}
